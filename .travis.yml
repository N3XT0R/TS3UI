language: php

php:
    - 5.4
    - 5.5
    - hhvm
    
install:
  - composer self-update
  - composer install
  
before_script:
     - cd tests
script:
    - phpunit 
    - php coverage-checker.php clover.xml 80
    
matrix:
  allow_failures:
    - php: hhvm